# DATABASE
spring.datasource.url=jdbc:mysql://localhost:3306/voting_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Sathish@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# DATE FORMAT FIX
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# FLYWAY
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
jwt.secret=VotingSystemSuperSecretKey256BitsLongForSecurity!@#$%
jwt.expiration=86400000

# AES
aes.secret.key=VotingAES256Key!!

# EMAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sathishkumar200456g@gmail.com
spring.mail.password=nunolrcwaaxkgyfp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# FILE UPLOAD
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
app.upload.dir=uploads/

# SERVER
server.port=8080
server.error.include-message=always

# Static resource serving for uploaded files
spring.web.resources.static-locations=classpath:/static/,file:uploads/
